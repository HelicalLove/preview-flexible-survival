'use babel';

import PreviewFlexibleSurvivalView from '../lib/preview-flexible-survival-view';

describe('PreviewFlexibleSurvivalView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
